// Edition 01 allocation. Authoritative value lives in the Shopify
// `manifest.allocation_total` metafield. This constant is the fallback
// used when the metafield is absent or unreachable.

export const EDITION_01_TOTAL = 1200;
export const EDITION_01_DEFAULT_ISSUED = 0;
export const EDITION_01_NUMBER = '01';
