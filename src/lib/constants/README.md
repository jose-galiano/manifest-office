# `src/lib/constants/` — Static values

**Rule:** numbers, regexes, and string literals that appear 2+ times in the
codebase. Includes the Manifest Office catalog (`products.ts`), commerce
constants (`commerce.ts` — free-ship threshold, engraving fee, cutoff hour),
and brand palette (`brand.ts` — ink, paper, signal, lichen).
