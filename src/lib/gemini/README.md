# `src/lib/gemini/` — Google Generative AI

**Rule:** wraps `generativelanguage.googleapis.com`.

Used by the Desk feature for trip-brief → kit-recommendation. Reads `GOOGLE_AI_API_KEY` from Vercel env.
